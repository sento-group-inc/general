# Codex デスクトップアプリ 設定・セキュリティ ガイド（研修教材／T9）

Day1 PMの習熟ブロックで使う。15人初学者が**最初に整えるべき設定**と、**セキュリティの基本**をまとめる。スクショ差し込みはT1/IMAGES側で対応。

---

## 0. 研修・推奨設定 早見表

| 項目 | 推奨 | 理由 |
|---|---|---|
| モデル | **GPT-5.4（中堅）** | 最上位は高品質だがトークンを食う。研修は15人×2日で**利用上限が早く来る**ので中堅で回す |
| 推論ティア | **中（既定）／ 軽い作業は低** | 「低」一律は本制作（提案書・技術資料）で品質低下。**「非常に高い」は研修で使わない**（トークン浪費） |
| 速度 | **標準** | 無駄なトークンを食わない |
| 作業モード | コーディング向け or **日常業務向け** | 非エンジニア中心なら「日常業務向け」も可（技術的細かさ控えめ・性能は同じ） |
| 実行中スリープ防止 | **ON** | 長い実行が途中で止まらない |
| ⌘+Enterで長文送信を必須 | **ON** | **誤送信を減らす**（書きかけの暴発防止） |
| デフォルト権限 | ON | ワークスペース内の読み書き。基本これで足りる |
| 自動レビュー | ON | 追加アクセス要求をCodexが自動レビュー（誤ることもある点は説明） |
| フルアクセス | **原則オフ寄りで運用** | 承認なしで全ファイル編集＋ネット実行。**データ損失・漏洩リスク**。§3参照 |
| 音声入力 | キー設定＋単語登録 | 会社名など固有名詞を登録して認識精度を上げる |
| メモリ（試験運用中） | **既定オフのまま**／Chronicleは触らない | メモリ注入はトークン増。蓄積は `learning-log/` で行う（§9） |
| ブラウザ デベロッパーモード（CDP） | **OFF固定** | フルCDP＝機密ブラウザ内部に到達・データ漏洩リスク（§10） |
| コンピュータ使用：ロック中の使用 | **OFF** | 不在時に勝手に動く。研修では不要（§11） |
| コンピュータ使用：Chrome連携 | 原則使わせない | 「あなたのアカウントとして」ログイン済みSaaSに入る（§11） |
| プラグイン：Box＋Microsoft365 | **有効化**（Box／Teams／SharePoint／Outlook） | ゼネラルのMicrosoft環境に直結。低権限・テスト範囲で（§12） |
| サイド／下部パネル | 触れる程度でOK | レビュー・ターミナル・ブラウザー・ファイルの場所だけ把握（§14・§15） |
| カスタム指示 | **おすすめを配布** | [custom-instructions-recommended.md](custom-instructions-recommended.md)（§13） |

---

## 1. モデルと推論ティア（トークンを守る）

- 推論ティア：**低 / 中 / 高 / 非常に高い**。深く考えるほど精度は上がるがトークンと時間を食う。
- 研修方針：**既定は「中」**。要約・整形・小さな修正など軽い作業は**「低」**に落とす。本当に詰まった難所だけ「高」。**「非常に高い」は使わない**。
- モデル：**GPT-5.4 系の中堅**で回す。利用上限に当たりやすい研修では、最上位を常用しない。
- 〔ねらい〕「賢さは無料じゃない。タスクに合わせてティアを変える」を体で覚えさせる。これ自体が判断の訓練。

---

## 2. 作業モード（一般 → 作業モード）

- **コーディング向け**：より技術的な応答と制御。
- **日常業務向け**：同じ性能で、技術的な細かさは控えめ。
- 〔研修〕部署混在で非エンジニアが多いなら「日常業務向け」から入ってもよい。性能は同じなので、表示の細かさの好みで選ぶ。

---

## 3. 権限＝セキュリティの本丸（一般 → 権限）

3段階。**ここが研修で一番大事なセキュリティの話**。

| 権限 | 何ができる | リスク |
|---|---|---|
| **デフォルトの権限** | ワークスペース内のファイルを読み書き。必要時に追加アクセスを要求 | 低。基本これで足りる |
| **自動レビュー** | 追加アクセス要求をCodexが自動で判断 | 中。**自動レビューは誤ることがある** |
| **フルアクセス** | 承認なしで**PC上の全ファイル編集＋ネットワーク実行** | 高。**データ損失・漏洩・予期しない動作** |

研修での教え方：
- 基本は**デフォルト権限**で十分、とまず伝える。
- フルアクセスは「承認をスキップして全部やらせる」モード。便利だが、**初学者が常時ONにするのは危険**。何が起きるか分かった上で、必要な場面だけ。
- 〔セキュリティ基本〕①機密情報（個人情報・顧客データ・社外秘）は安易に渡さない ②APIキー・PAT・パスワードは**プレースホルダ**（`YOUR_XXX`）で扱い、AIに代行入力させない ③出力は鵜呑みにせず確認・レビューする。

---

## 4. メニュー機能（＋ボタン／作成・プラグイン）

- **プランモード**：いきなり実行せず、**先に計画を立てさせる**読み取り中心モード。大きめの作業の前に使うと暴走しない。
- **目標を目指す（目標モード）**：ゴールを与えて自律的に進ませる。長時間タスク向け。研修では「こういうモードもある」程度に。
- **IDEコンテキストを含める**：Cursor等のIDE側の文脈を取り込む。今回はCursorを使わない方針なので**基本オフでよい**（混乱を避ける）。
- **作成**：成果物を直接生成。
  - **プレゼンテーション** → 提案書のスライド寄り
  - **ドキュメント** → 技術資料・利用方針・月次報告
  - **スプレッドシート** → 一覧・TSV・集計
  - 〔本制作との接続〕Day2のアウトプットはこの「作成」から入ると速い。
- **プラグイン**：拡張。研修では深入りしない。

---

## 5. 一般設定の要点（一般タブ）

- **デフォルトで開く場所**：ファイル/フォルダを開くアプリ。今回はCursorを常用しないので、ここは各自の環境次第（必須ではない）。
- **言語**：日本語（日本）でOK。
- **メニューバーに表示**：ウィンドウを閉じてもmacのメニューバーに残す（mac勢向け。Windowsは該当UIが異なる）。
- **下部パネル**：ヘッダーに下部パネルのコントロールを表示。
- **既定のターミナルの場所**：下／右。統合ターミナルの出る場所。
- **実行中はスリープを防止**：**ON**。実行を止めない。
- **速度**：**標準**。チャット・サブエージェント・コンパクションで共通の推論ティア。無駄にトークンを食わない。
- **コードレビュー（インライン / 独立）**：
  - **インライン**：いまのチャットの中で `/review` を走らせる。文脈を引き継いで軽く見たいとき。
  - **独立**：別のレビュー専用チャットを立てる。元の作業を汚さず、レビューだけ切り離したいとき。
  - 〔研修〕まずは**インライン**で十分。大きめの成果物を集中レビューしたいときだけ独立。
- **おすすめのプロンプト**：プロジェクトファイルと接続済みアプリを見て次の一手を提案。ONで初学者の取っ掛かりになる。
- **設定をインポート**：他のAIアプリから設定・プロジェクト・最近のチャットを引き継ぐ。

---

## 6. コンポーザー設定

- **コンテキストウィンドウの使用量を表示**：ON推奨。どれだけ文脈を食っているか見える＝トークン感覚が育つ。
- **フォローアップの動作（キュー / ステア）**：
  - **キュー**：実行中の追加入力を順番待ちに積む。
  - **ステア**：実行中の処理に割り込んで方向修正。
  - 〔研修〕既定の**キュー**で十分。
- **⌘+Enterでの長文送信を必須**：**ON**。複数行は⌘+Enterでしか送れなくなる＝**書きかけの誤送信を防ぐ**。

---

## 7. 音声入力（推奨セットアップ）

- 音声入力の**キーを設定**して有効化する。タイピングが速くない人ほど効く（拡張感覚の一種）。
- **よく使う単語を登録**：会社名・部署名・製品名・専門用語など固有名詞を辞書登録して認識精度を上げる。
  - 例：「ゼネラル」「セントグループ」等。
- 〔ねらい〕入力のハードルを下げ、AIに渡す量を増やす。インプットが増えれば出力が良くなる（§性格診断の体験と地続き）。

---

## 8. セキュリティ基本（まとめ・研修で必ず触れる）

1. **機密情報を安易に渡さない**：個人情報・顧客データ・社外秘は入れる前に一拍置く。
2. **鍵はプレースホルダ**：APIキー/PAT/パスワードは `YOUR_XXX`。AIに代行入力させない。
3. **権限は最小から**：デフォルト権限で始め、フルアクセスはリスクを理解した上で必要時のみ。
4. **出力を確認する**：AIの結果は鵜呑みにせず、人がレビューしてから使う。
5. **成果物の置き場**：SharePointの個別フォルダへ。共有ルールを守る。

---

## 9. メモリ（試験運用中）

3トグル。設定画面を図解した日本語記事はほぼ無いので**スクショは自前で撮る**（公式: https://developers.openai.com/codex/memories ）。

- **メモリを有効にする**：チャットから新しいメモリを作り、新しいチャットに引き継ぐ（`memories=true`）。**既定オフ**（EEA/UK/スイスは明示オプトイン必須）。
- **ツール使用チャットをスキップ**：MCPツール／Web検索を使ったチャットからはメモリを生成しない（肥大化防止）。
- **メモリをリセット**：`~/.codex/memories/` を全消去（可逆）。
- 〔研修方針〕**既定オフのまま**でよい。学びの蓄積は本研修では `learning-log/`＋振り返りskillで行う（§AGENTS.mdと同じ思想）。メモリ注入は入力トークンを増やすので、トークンを守る今回は積極利用しない。**Chronicle（試験運用）は公式が「レート上限を急速に消費」と警告**＝触らない。

## 10. ブラウザ操作（ブラウザ タブ）

Codexの組み込みブラウザ操作（公式: https://developers.openai.com/codex/app/browser ）。

- **組み込みブラウザの操作を許可**：`@Browser` で呼び出し。アプリ内ブラウザはログイン済みセッション・cookie・拡張を**共有しない（隔離）**＝安全側。
- **Default local URL open destination**：localhost等をどこで開くか（Codex内で開く設定）。
- **閲覧データ**：アプリ内ブラウザのデータ/キャッシュ消去。
- **注釈のスクリーンショット**：要素にコメントを渡せて理解が上がるが、**プラン使用量（トークン）が増える**。研修では「常に含める」を多用しない。
- **承認**：「常に確認」＝未知サイトは毎回承認を求める。**この毎回確認を維持**。allowedリストへ安易に足さない。
- **デベロッパーモード（CDPフルアクセス）**：**OFF固定**。公式が「機密性の高いブラウザ内部を検査・制御＝データを危険に晒す」と明言。

## 11. コンピュータ使用（コンピュータの使用）

Codexが**他のアプリ**を操作する機能（公式: https://developers.openai.com/codex/app/computer-use ）。**今回の研修で最大のセキュリティ注意領域**。

- **任意のアプリ操作**：画面収録＋アクセシビリティ権限で、人が見える/触れるものをGUI操作。
- **Google Chrome連携**：拡張で接続。公式原文「**操作はあなたのアカウントとして扱われる**」＝ログイン済みSaaSに再認証なしで入る。
- **ロック中の使用**：Macロック中でも動かす（EEA/UK/スイス未提供）。**OFF**。
- 〔研修方針〕基本は**使わない／OFF**。使うとしてもデモ程度。**Chrome連携とロック中使用が、不在・認証という人間の油断を直撃する設計**である点を必ず説明。機密タスクには使わない（公式も非推奨）。

## 12. プラグイン（コネクタ）

「お気に入りのツールでCodexを活用」（公式: https://developers.openai.com/codex/plugins ）。Plugins → ディレクトリ → Install → OAuth承認。資格情報はOSキーストア保存。

- **Box＝有効化する**（ゼネラルが利用中）。「Search and reference your documents」。社内ドキュメントをCodexから参照・引用できる。本制作のインプット源になる。
  - 〔注意〕**テスト範囲・低権限**で接続。不可逆操作（削除・上書き）は人間承認。研修後にConnectorsからDisconnectしてよい。
- **Microsoft 365コネクタ＝有効化する**（ゼネラルのスタックに直結。Googleではなくこちらを使う）。各「プラグインを追加」→OAuth承認。
  - **Teams**（Summarize Teams and draft follow-ups）：チャネル/会議の要約、フォローアップの下書き。
  - **SharePoint**（Summarize SharePoint sites and files）：サイト・ファイルの要約。成果物提出先でもあり、参照元としても効く。
  - **Outlook Email**（Triage Outlook inboxes and draft replies）：受信箱の仕分け・返信下書き。
  - **Outlook Calendar**（Manage Outlook schedules and meeting changes）：予定の確認・調整。
  - 〔本制作との接続〕「Teamsの議論を要約→議事録」「SharePointの過去資料を参照→提案書」など、業務インプットがそのまま入る。
- その他：Googleドキュメント/スプレッドシート/スライド・Gmail・Google Drive 等は**今回Google系は使わない方針なので無効のまま**。GitHubは必要な課題でのみ。
- 〔研修方針〕**第三者製MCPは入れない**（公式Codexプラグイン優先）。コネクタは読み書き権限が初期で広いことがあるので**最小付与・テスト範囲**。不可逆操作（メール送信・予定変更・削除）は人間承認。研修後はDisconnect可。

## 13. カスタム指示・パーソナライゼーション

**重要な構造**：アプリの「カスタム指示／パーソナライゼーション」編集＝**`~/.codex/AGENTS.md`（個人グローバル指示）への書き込み**（公式: https://developers.openai.com/codex/concepts/customization ）。設定画面とAGENTS.mdは「同じファイルの表と裏」。

- 設定場所：右下アカウント名 → Personalization → Personality（Friendly / Pragmatic / None）＋ Custom instructions欄。
- 2層の優先順位：**global `~/.codex/AGENTS.md`（個人・全プロジェクト）< project `AGENTS.md`（リポジトリ直下・チーム規約）**。作業ディレクトリに近い方が優先。
  - 個人の好み（口調・冗長度・既定の進め方）→ global。
  - チーム/案件のルール → project AGENTS.md。
- 〔研修方針〕**おすすめのカスタム指示を配布**して全員のglobalに入れてもらう → [custom-instructions-recommended.md](custom-instructions-recommended.md)。出力の質と安全性の初期値が揃う。

---

## 出典メモ（教材作成用）

設定パネルを図解した日本語記事はほぼ無い。要点の出典は公式doc（developers.openai.com/codex）。用語整理に [npaka 4機能の違い](https://note.com/npaka/n/n6ea96a2c929c)、コンピュータ使用の和文スクショに [npaka computer use](https://note.com/npaka/n/nd62f3e1fe802)、コネクタ一覧に [fyve](https://fyve.co.jp/codex/articles/codex-plugins-90-overview)。**設定画面のスクショは自分のCodexアプリで撮るのが最短**（T1/IMAGESで対応）。

---

## 13b. AGENTS.md の育て方（永続化）

AGENTS.md＝AIへの指示書。**うまくいった指示はここに書いて永続化**し、毎回手で書く無駄をなくす（出典: [ai-driven note](https://note.com/ai_driven/n/n2e2516c28c16)・[公式 agents-md](https://developers.openai.com/codex/guides/agents-md)）。

- **`/init` で雛形を生成** → そこから編集。あくまで出発点で、チームの実態（どうビルド・テスト・レビュー・デプロイするか）に合わせて直すのが肝。
- **良いAGENTS.mdがカバーするもの**：
  1. リポジトリのレイアウトと重要なディレクトリ
  2. プロジェクトの実行方法
  3. ビルド・テスト・リントのコマンド
  4. エンジニアリング規約とPRの期待値
  5. やってはいけないこと（制約とルール）
  6. 「完了」の定義と検証方法
- **改善サイクル**：Codexが同じミスを2回したら、レトロして**摩擦に基づきルールを更新**。
- **活動ログは書かない**：生ログは `learning-log/`。再利用できるルールになったものだけAGENTS.mdへ昇格（32KiB制限）。

> 非エンジニア業務なら 1〜3 は「フォルダ構成・成果物の置き場・使う手順」に読み替えてよい。要は「このプロジェクトでAIに毎回守ってほしい前提」を書く。

## 13c. Planモードを使う

大きめ・曖昧な作業は、いきなり実行させず **`/plan`（または＋メニューのプランモード）で先に計画を立てさせる**。手順を確認してから走らせると暴走・的外れが激減する。詳細は [slash-commands.md](slash-commands.md)。

## 14. サイドパネル（場所だけ把握すればOK）

> 全部は説明しない。「どこに何があるか」だけ。スクショを残す。

- **レビュー**（⌃⇧G）：変更内容を見る／`/review` を走らせる。
- **ターミナル**：統合ターミナル。コマンド実行・結果確認。
- **ブラウザー**（⌘T）：組み込みブラウザ（§10）。
- **ファイル**（⌘P）：ファイルを開く・切り替える。

## 15. 下部パネル（ターミナル）

- アプリ下部に**プロジェクト名のタブ**（例：`Aigassyuku06`）でターミナルが出る。Codexが叩いたコマンドや、自分で打つコマンドがここで動く。
- 「下部パネル」を一般設定でON（§5）にすると、ヘッダーから開閉できる。
- 〔研修〕「AIがここで実際にコマンドを動かしている」が見える場所、とだけ伝える。中身の細部は飛ばす。
